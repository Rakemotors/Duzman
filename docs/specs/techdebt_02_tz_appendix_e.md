# techdebt_02: TZ Приложение Е, удаление стейл-маркера v1.3

Based on: Техническое задание v1.7 от 2026-05-19 (docs/TZ.md)

## Issue

Closes #9

## Контекст

В Приложении Е docs/TZ.md строка вводного абзаца содержит маркер «Новое в v1.3», тогда как само приложение уже существенно переписано в v1.4 и v1.7 (см. журнал изменений в шапке TZ: строки 26, 29, 32 и таблицу версий внизу). Маркер вводит в заблуждение и должен быть убран.

Остальные вхождения `v1.3` в docs/TZ.md являются легитимными историческими маркерами (журнал изменений, описания «что появилось в v1.3» для dev/prod, ingestion endpoints, runtime, ARCHITECTURE.md и т.д.) и в скоуп этой спеки НЕ входят.

## Зона спецификации

- docs/TZ.md

Файлы вне этого списка изменяться не должны.

## Что нужно сделать

В docs/TZ.md заменить строку

```
Новое в v1.3. Формализует роли и взаимодействие между Claude, Claude Code, Codex CLI и ChatGPT.
```

на

```
Формализует роли и взаимодействие между Claude, Claude Code, Codex CLI и ChatGPT.
```

То есть удалить префикс «Новое в v1.3. » из вводного абзаца Приложения Е. Остальной текст приложения не трогать.

## Definition of done

- [ ] В docs/TZ.md больше нет подстроки `Новое в v1.3. Формализует роли`.
- [ ] В docs/TZ.md присутствует строка `Формализует роли и взаимодействие между Claude, Claude Code, Codex CLI и ChatGPT.` (без префикса «Новое в v1.3. »).
- [ ] Заголовок `## Приложение Е. Процесс работы нескольких AI-агентов` сохранён без изменений.
- [ ] Раздел Е.2 сохраняет фразу «на момент 19 мая 2026 это v1.7» (это легитимная отсылка, НЕ трогаем).
- [ ] Журнал изменений в шапке TZ и таблица версий внизу TZ не изменены.
- [ ] Других файлов PR не трогает.
- [ ] Тесты зелёные: `.venv/bin/python -m pytest -q`.

## Проверки

```
grep -n "Новое в v1.3. Формализует роли" docs/TZ.md
```
Ожидается: пусто (нет вхождений).

```
grep -n "Формализует роли и взаимодействие" docs/TZ.md
```
Ожидается: ровно одна строка, без префикса «Новое в v1.3. ».

```
grep -n "## Приложение Е" docs/TZ.md
```
Ожидается: одна строка с заголовком приложения.

```
grep -n "на момент 19 мая 2026 это v1.7" docs/TZ.md
```
Ожидается: одна строка (отсылка в Е.2 сохранена).

```
git diff --stat
```
Ожидается: изменён только docs/TZ.md, объём правки около 1 строки.

```
.venv/bin/python -m pytest -q
```
Ожидается: 268 passed.

## Ветка и PR

- Ветка: `techdebt/09-tz-appendix-e-stale-v13`
- Заголовок PR: `docs: remove stale v1.3 marker from TZ Appendix E (#9)`
- Тело PR заполнить по шаблону, в Definition of done и Проверки скопировать пункты из этой спеки.

## Forbidden actions

Стандартный набор из Приложения Ж docs/TZ.md. Дополнительно:
- НЕ трогать журнал изменений в шапке TZ.
- НЕ трогать таблицу версий внизу TZ.
- НЕ трогать другие вхождения `v1.3` в docs/TZ.md.
- НЕ менять никакие файлы кроме docs/TZ.md.
